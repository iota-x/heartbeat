# websocket URL of the ingest service (server/). unset → the client runs on
# its synthetic feed. never put API keys here — NEXT_PUBLIC_* ships to the
# browser.
#NEXT_PUBLIC_FEED_URL=ws://localhost:8787
